package packagename

import "Door.gcd"


adapter DoorAdapter of component door : Door [] {
	
	when any / run

	queue internalQueue(priority = 2, capacity = 4) {
		ControlActions.any
	}
	
	queue environmentalQueue(priority = 1, capacity = 4) {
		OpeningTimeout.any, ClosingTimeout.any
	}

}
