package functions

interface Execute {
	out event ^execute
}

interface FunctionExecute extends Execute {
	procedure setValue(x : integer) : void
	lambda getValue : integer
	
	procedure setRecord(r : Record) : void
	lambda getRecord : Record
	
	lambda factorial(value : integer) : integer := (value <= 1) ? 1 : value * factorial(value - 1)
}

type Record : record {
	a : boolean,
	b : integer
}

type ComplexRecord : record {
	a : decimal,
	b : Record
}

type int : integer

type EnumTest : enum {
	_A, _B
}

type ArrayEnumRecord : record {
	a : [2]int,
	b : EnumTest
}