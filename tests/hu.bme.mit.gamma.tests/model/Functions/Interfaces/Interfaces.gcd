package nondetchoice

interface Execute {
	out event ^execute
	out event ^execute_2
	out event ^execute_3
}

type Record : record {
	a : boolean,
	b : integer
}

type ComplexRecord : record {
	a : decimal,
	b : Record
}