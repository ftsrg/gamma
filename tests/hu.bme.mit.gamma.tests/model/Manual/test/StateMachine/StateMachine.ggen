import "model/StateMachine/StateMachine"
import "model/StateMachine/StateMachineWrapper"
import trace "StateMachineTest.get"

code {
	component: StateMachine
	language: java
}

code {
	component: StateMachineWrapper
	language: java
}

test {
	trace: StateMachineTest
	file: "StateMachineTest"
	language: java
}
